[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx tsx server.ts --help || true"

[deploy]
startCommand = "npx tsx server.ts"
healthcheckPath = "/api/movies/upcoming"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
